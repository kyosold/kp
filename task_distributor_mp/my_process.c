#include <stdio.h>
#include <string.h>
#include <errno.h>
#include "my_process.h"

/**
 * @brief 
 * 
 * @param buf 
 * @param buf_len 
 * @return int Return 0 is succ. 1 is error
 */
int my_process(char *buf, size_t buf_len)
{
    return K_SUCC;
}